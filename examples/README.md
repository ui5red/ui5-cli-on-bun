# Examples

This directory contains example applications copied from the sibling UI5 CLI repository.

They are kept separate from the automated `test/` fixtures so they can be explored or run manually without mixing them into the aggregate fixture suite.

Current contents:

- `browsersync`
