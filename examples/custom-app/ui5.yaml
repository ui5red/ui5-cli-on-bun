---
specVersion: "4.0"
type: application
metadata:
  name: ui5bun.example.customapp
server:
  customMiddleware:
    - name: ui5-cli-on-bun-middleware
      afterMiddleware: compression
builder:
  customTasks:
    - name: ui5-cli-on-bun-task
      afterTask: minify