sap.ui.define([], () => {
	"use strict";

	const thisLib = {};

	sap.ui.getCore().initLibrary({
		name: "theme.heavy.library",
		version: "1.0.0",
		dependencies: [],
		noLibraryCSS: false,
	});

	return thisLib;
});