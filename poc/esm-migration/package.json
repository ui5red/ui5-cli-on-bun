{
	"name": "ui5-esm-migration",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "POC: migrating UI5 AMD modules to native ES Modules",
	"scripts": {
		"build:ui5": "ui5 build --all --dest dist --clean-dest",
		"build:esm": "bun run build:ui5 && cp -r dist/ dist-esm/ && cp -r esm-overlay/* dist-esm/",
		"serve:ui5": "bun run serve.js --dir dist --port 8080",
		"serve:esm": "bun run serve.js --dir dist-esm --port 8080",
		"test:esm-modules": "bun run test-esm-modules.js",
		"test:esm-bridge": "bun run test-esm-bridge.js",
		"test:esm-build": "bun run build-esm.js",
		"test": "bun run test:esm-modules && bun run test:esm-bridge && bun run test:esm-build",
		"clean": "rm -rf dist dist-esm build-output"
	},
	"devDependencies": {
		"@openui5/types": "^1.147.0",
		"@ui5/cli": "^4.0.26",
		"typescript": "^5.9.3",
		"ui5-tooling-transpile": "^3.9.2"
	}
}
