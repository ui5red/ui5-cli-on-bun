<mvc:View
	controllerName="sample.ts.app.controller.Main"
	displayBlock="true"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	core:require="{
		formatter: 'sample/ts/app/model/formatter'
	}">

	<Page
		title="{i18n>appTitle}"
		id="page">
		<content>
			<VBox>
				<MessageStrip
					visible="{runtime>/isBun}"
					showIcon="true"
					type="Success"
					text="{i18n>bunRuntimeBanner}" />
				<IllustratedMessage
					title="{i18n>appTitle}"
					illustrationType="sapIllus-SuccessHighFive"
					enableVerticalResponsiveness="true"
					description="{i18n>appDescription}">
					<additionalContent>
						<Button
							id="helloButton"
							text="{formatter: 'formatter.formatValue', path: 'i18n>btnText'}"
							press=".sayHello" />
					</additionalContent>
				</IllustratedMessage>
			</VBox>
		</content>
	</Page>

</mvc:View>
